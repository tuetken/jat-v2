# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=your-project-url

# Your Supabase anon/public key (safe to expose in client-side code)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# CRITICAL: Service role key should NOT be used for standard CRUD operations.
# Only include if needed for exceptional non-user-scoped operations (e.g., admin tasks).
# If included, isolate its usage and never expose to client-side code.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
